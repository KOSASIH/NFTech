import React from 'react';

const Notification = () => {
    return (
        <div className="notification">
            <p>Get notified about new updates and features!</p>
            <button>Subscribe</button>
        </div>
    );
};

export default Notification;
